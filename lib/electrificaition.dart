import 'package:flutter/material.dart';

class Electrification extends StatelessWidget {
  const Electrification({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Electrificatin"),
        elevation: 0.0,
        toolbarHeight: 65.0,
      ),
    );
  }
}
